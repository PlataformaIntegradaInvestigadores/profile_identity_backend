services:
  profile-identity-web:
    build: .
    command: python manage.py runserver 0.0.0.0:8002
    volumes:
      - .:/app
    ports:
      - "8002:8002"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      - profile-identity-db
    env_file:
      - .env
    environment:
      - DB_HOST=profile-identity-db
      - DB_PORT=5432
    networks:
      - default
      - centinela-net

  profile-identity-db:
    image: postgres:16
    ports:
      - "5434:5432"
    volumes:
      - profile_identity_postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_DB=${DB_NAME:-profile_identity}
      - POSTGRES_USER=${DB_USER:-profile_identity}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-profile_identity}
    networks:
      - default
      - centinela-net

volumes:
  profile_identity_postgres_data:

networks:
  centinela-net:
    external: true
